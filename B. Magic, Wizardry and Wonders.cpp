#include <iostream>
#include <stdio.h>
#define ll long long int

using namespace std;

int main(void)
{
    ll n=0, d=0, l=0;
    cin >> n >> d >> l;
    if (n >= 2 && l >= 1)
    {

    }
    else
    {
        cout << "-1";
    }
    printf ("\n");
}